apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: frontpage
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: frontpage
        commit: '${WERCKER_GIT_COMMIT}'
    spec:
      containers:
      - name: frontpage
        image: docker.io/eminami69/mfrontpage
        ports:
        - containerPort: 80
        resources:
          requests: 
            memory: 500Mi
            cpu: 0.5
          limits:
            memory: 500Mi
            cpu: 0.5
        imagePullPolicy: Always
      imagePullSecrets:
      - name: wrelease
